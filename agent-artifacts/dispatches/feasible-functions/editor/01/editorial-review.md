# Editorial review: dispatches/feasible-functions (01)

## Decision

APPROVE

## Review

The article teaches the method in the commissioned order. The CNP equation is the explanatory center, and its projection, convex-vertex, and nonnegative-ray terms are defined before the equation is interpreted. The pointwise table makes the square, `sin^2`, and stick-breaking constructions concrete. The exact-reduction section identifies the narrower `u = G(y)` condition, the Poisson parameterization, automatic differentiation, and the `H^2` regularity boundary. The final section reports the L-shaped-domain and integral-constraint metrics with example scope and explicitly preserves the complementarity penalty exception.

The draft keeps feasibility, density, and optimizer convergence separate. It does not turn the numerical comparisons into a universal ranking, and it does not describe hPINN's penalty/augmented-Lagrangian approach as the same kind of hard architectural constraint. The nine source entries are primary or official sources and are cited in first-use order.

## Direct edits

No substantive prose edit was required in this round. The stamped header now reads 8 minutes, matching the computed metadata. The annotated equation's TeX triggers the repository's sentence-density heuristic; that warning is expected for the equation text and does not indicate a prose sentence.

## Fresh proof

`nb stamp` result: `words=1736 reading_minutes=8 sources=9`.

`nb check` result: `BLOCK: 0`, verdict `PUBLISHABLE`.

Remaining warning: one equation-density heuristic warning only.
